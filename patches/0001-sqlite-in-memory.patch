diff --git a/danmakus-core/src/core/BunSqliteLiveSessionOutbox.test.ts b/danmakus-core/src/core/BunSqliteLiveSessionOutbox.test.ts
index 63fb355..6ecf99c 100644
--- a/danmakus-core/src/core/BunSqliteLiveSessionOutbox.test.ts
+++ b/danmakus-core/src/core/BunSqliteLiveSessionOutbox.test.ts
@@ -1,9 +1,4 @@
 import { describe, expect, it } from 'bun:test';
-import { Database } from 'bun:sqlite';
-import { randomUUID } from 'node:crypto';
-import { existsSync, writeFileSync } from 'node:fs';
-import { tmpdir } from 'node:os';
-import { join } from 'node:path';
 import { createBunSqliteLiveSessionOutbox } from './BunSqliteLiveSessionOutbox.js';
 import {
   createResettableSqliteLiveSessionOutboxBackend,
@@ -17,13 +12,9 @@ import {
 const TEST_STREAMER_UID = 84;
 
 describe('BunSqliteLiveSessionOutbox', () => {
-  it('persists pending records in a sqlite file', async () => {
+  it('persists pending records in memory', async () => {
     const nowMs = Date.now();
-    const databasePath = join(
-      tmpdir(),
-      `danmakus-live-session-outbox-${randomUUID()}.sqlite3`,
-    );
-    const outbox = createBunSqliteLiveSessionOutbox({ databasePath });
+    const outbox = createBunSqliteLiveSessionOutbox();
 
     await outbox.append([
       {
@@ -38,36 +29,8 @@ describe('BunSqliteLiveSessionOutbox', () => {
     expect(dueRecords[0]?.payload).toEqual(new Uint8Array([0, 1, 2, 3, 253, 254, 255]));
     expect(await outbox.countPending()).toBe(1);
 
-    const reopenedOutbox = createBunSqliteLiveSessionOutbox({ databasePath });
-    expect(await reopenedOutbox.countPending()).toBe(1);
-
-    await reopenedOutbox.ack([dueRecords[0]!.id]);
-    expect(await reopenedOutbox.countPending()).toBe(0);
-    expect(existsSync(`${databasePath}-wal`)).toBe(false);
-  });
-
-  it('rebuilds the database automatically when the sqlite file is corrupted', async () => {
-    const nowMs = Date.now();
-    const databasePath = join(
-      tmpdir(),
-      `danmakus-live-session-outbox-corrupted-${randomUUID()}.sqlite3`,
-    );
-
-    writeFileSync(databasePath, 'not a sqlite database');
-
-    const outbox = createBunSqliteLiveSessionOutbox({ databasePath });
-
-    await outbox.append([
-      {
-        streamerUid: TEST_STREAMER_UID,
-        eventTsMs: nowMs,
-        payload: new Uint8Array([9, 8, 7]),
-      },
-    ]);
-
-    const dueRecords = await outbox.listDue({ nowMs, limit: 10 });
-    expect(dueRecords).toHaveLength(1);
-    expect(dueRecords[0]?.payload).toEqual(new Uint8Array([9, 8, 7]));
+    await outbox.ack([dueRecords[0]!.id]);
+    expect(await outbox.countPending()).toBe(0);
   });
 
   it('reconnects once before rebuilding a locked sqlite database', async () => {
@@ -147,44 +110,6 @@ describe('BunSqliteLiveSessionOutbox', () => {
     expect(isSqliteResettableError(error)).toBe(true);
   });
 
-  it('deletes the sqlite database when the schema version does not match', async () => {
-    const nowMs = Date.now();
-    const databasePath = join(
-      tmpdir(),
-      `danmakus-live-session-outbox-schema-${randomUUID()}.sqlite3`,
-    );
-    const db = new Database(databasePath, { create: true });
-    db.exec(`
-      CREATE TABLE live_session_outbox (
-        id INTEGER PRIMARY KEY AUTOINCREMENT,
-        streamer_uid INTEGER NOT NULL,
-        event_ts_ms INTEGER NOT NULL,
-        payload TEXT NOT NULL
-      );
-      PRAGMA user_version = 2;
-    `);
-    db.query(`
-      INSERT INTO live_session_outbox (streamer_uid, event_ts_ms, payload)
-      VALUES (?, ?, ?)
-    `).run(TEST_STREAMER_UID, nowMs, '[0,1,2]');
-    db.close();
-
-    const outbox = createBunSqliteLiveSessionOutbox({ databasePath });
-    expect(await outbox.countPending()).toBe(0);
-
-    await outbox.append([
-      {
-        streamerUid: TEST_STREAMER_UID,
-        eventTsMs: nowMs,
-        payload: new Uint8Array([4, 5, 6]),
-      },
-    ]);
-
-    const dueRecords = await outbox.listDue({ nowMs, limit: 10 });
-    expect(dueRecords).toHaveLength(1);
-    expect(dueRecords[0]?.payload).toEqual(new Uint8Array([4, 5, 6]));
-  });
-
   it('accepts blob payload values returned by sqlite adapters', async () => {
     const nowMs = Date.now();
     const backend: SqliteLiveSessionOutboxBackend = {
diff --git a/danmakus-core/src/core/BunSqliteLiveSessionOutbox.ts b/danmakus-core/src/core/BunSqliteLiveSessionOutbox.ts
index 9ff20c4..9f8fa4a 100644
--- a/danmakus-core/src/core/BunSqliteLiveSessionOutbox.ts
+++ b/danmakus-core/src/core/BunSqliteLiveSessionOutbox.ts
@@ -1,5 +1,3 @@
-import { mkdirSync, readFileSync, rmSync } from 'node:fs';
-import { dirname, resolve } from 'node:path';
 import type { LiveSessionOutboxStore } from './LocalArchiveTypes.js';
 import {
   createResettableSqliteLiveSessionOutboxBackend,
@@ -9,12 +7,6 @@ import {
   type SqliteLiveSessionOutboxValue,
 } from './SqliteLiveSessionOutbox.js';
 
-export const DEFAULT_BUN_LIVE_SESSION_OUTBOX_PATH = resolve(
-  process.cwd(),
-  '.danmakus',
-  'danmakus-live-session-outbox-streamer.sqlite3',
-);
-
 type BunSqliteDatabase = {
   exec(sql: string): void;
   run(sql: string, ...params: SqliteLiveSessionOutboxValue[][]): {
@@ -44,33 +36,13 @@ const loadBunSqlite = async (): Promise<{
   };
 };
 
-const deleteSqliteDatabaseFiles = (databasePath: string): void => {
-  rmSync(databasePath, { force: true });
-  rmSync(`${databasePath}-wal`, { force: true });
-  rmSync(`${databasePath}-shm`, { force: true });
-  rmSync(`${databasePath}-journal`, { force: true });
-};
-
-const hasValidSqliteHeader = (databasePath: string): boolean => {
-  try {
-    const header = readFileSync(databasePath, { encoding: 'utf8', flag: 'r' }).slice(0, 16);
-    return header === 'SQLite format 3\u0000';
-  } catch {
-    return true;
-  }
-};
-
-const createBunSqliteBackend = async (databasePath: string): Promise<ResettableSqliteLiveSessionOutboxBackend> => {
-  mkdirSync(dirname(databasePath), { recursive: true });
+const createBunSqliteBackend = async (): Promise<ResettableSqliteLiveSessionOutboxBackend> => {
   const { Database } = await loadBunSqlite();
-  if (!hasValidSqliteHeader(databasePath)) {
-    deleteSqliteDatabaseFiles(databasePath);
-  }
   const openDatabase = (): BunSqliteDatabase => {
-    const db = new Database(databasePath, { create: true });
+    const db = new Database(':memory:');
     db.exec('SELECT 1;');
-    db.exec('PRAGMA journal_mode = DELETE;');
-    db.exec('PRAGMA synchronous = NORMAL;');
+    db.exec('PRAGMA journal_mode = OFF;');
+    db.exec('PRAGMA synchronous = OFF;');
     db.exec('PRAGMA busy_timeout = 30000;');
     return db;
   };
@@ -82,7 +54,6 @@ const createBunSqliteBackend = async (databasePath: string): Promise<ResettableS
     if (!isSqliteCorruptionError(error)) {
       throw error;
     }
-    deleteSqliteDatabaseFiles(databasePath);
     db = openDatabase();
   }
 
@@ -108,18 +79,15 @@ const createBunSqliteBackend = async (databasePath: string): Promise<ResettableS
 
     reset: async (): Promise<void> => {
       db.close(true);
-      deleteSqliteDatabaseFiles(databasePath);
+      db = openDatabase();
     },
   };
 };
 
-export const createBunSqliteLiveSessionOutbox = (options?: {
-  databasePath?: string;
-}): LiveSessionOutboxStore => {
-  const databasePath = resolve(options?.databasePath ?? DEFAULT_BUN_LIVE_SESSION_OUTBOX_PATH);
+export const createBunSqliteLiveSessionOutbox = (): LiveSessionOutboxStore => {
   return createSqliteLiveSessionOutbox(
     createResettableSqliteLiveSessionOutboxBackend(
-      async () => await createBunSqliteBackend(databasePath),
+      async () => await createBunSqliteBackend(),
     ),
   );
 };
